: Error response from daemon: invalid mount config for type "bind": bind source path does not exist: /dev/noexist.
You can use the
bind-create-src
option to automatically create the source directory
on the host if it doesn't exist:
$
docker run --mount
type
=
bind,src
=
/home/user/mydir,dst
=
/mnt/foo,bind-create-src alpine
Options for --mount
The
--mount
flag consists of multiple key-value pairs, separated by commas
and each consisting of a
<key>=<value>
tuple. The order of the keys isn't
signi