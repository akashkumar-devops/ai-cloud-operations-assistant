at take
effect when the image starts up as a container. When you run an image, you can
override those defaults using flags for the
docker run
command.
Default entrypoint
Default command and options
Expose ports
Environment variables
Healthcheck
User
Working directory
Default command and options
The command syntax for
docker run
supports optionally specifying commands and
arguments to the container's entrypoint, represented as
[COMMAND]
and
[ARG...]
in the following synopsis example:
$
docker run